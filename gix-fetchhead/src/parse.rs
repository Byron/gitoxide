
use crate::FetchHead;

pub fn parse(/*Repository Type?*/) -> FetchHead {
    // let fetch_head_file = repo_dir.join(".git").join("FETCH_HEAD");
    // std::fs::File::open(&ignore_file)?.read_to_end(&mut buf)?;
    todo!();
}
